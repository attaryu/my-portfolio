# self-components folder

This folder contains only the main page components (portfolio). So, it's not a shared component. The reason I made it a component is because like more "independent" between animations.